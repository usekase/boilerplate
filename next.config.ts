/**
 * Next.js Configuration
 * =====================
 *
 * This file configures Next.js behavior for your application.
 * Most settings are left as defaults for simplicity.
 *
 * WORKSHOP TIP: You typically don't need to modify this file.
 * If you need to add environment variables or configure domains
 * for images, this is where you'd do it.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Add configuration options here if needed */
};

export default nextConfig;
