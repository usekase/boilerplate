/**
 * Providers Component
 *
 * Wraps the app with necessary providers.
 * Add theme, state management, or other providers here.
 */

"use client";

export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
